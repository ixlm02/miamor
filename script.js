function showAlert(notice) {
    if (notice === 'one') {
        alert("Mi amor, Esha Wahid.");
    } 
     else if (notice === 'two') {
        alert("Currently on home page.");
    }
     else (notice === 'three'); {
        alert("Stay tuned for the update.");
     }
}
